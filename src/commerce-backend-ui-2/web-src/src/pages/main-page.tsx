import { JuspayConfig } from "#web/components/juspay-config.tsx";

/** Entrypoint for the main page of the app. */
export function MainPage() {
  return (
    <main>
      <JuspayConfig />
    </main>
  );
}
